apiVersion: v1
kind: Service
metadata:
  name: catsdogs-inference
  labels:
    app: catsdogs-inference
spec:
  type: NodePort
  selector:
    app: catsdogs-inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
