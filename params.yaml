# Hyper-parameters consumed by the DVC pipeline / training script.
preprocess:
  image_size: 224
  split:
    train: 0.8
    val: 0.1
    test: 0.1
  seed: 42

train:
  epochs: 5
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  seed: 42
